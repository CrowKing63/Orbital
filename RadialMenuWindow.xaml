<Window x:Class="Orbital.RadialMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OrbitalMenu"
        SizeToContent="WidthAndHeight"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        ShowActivated="False"
        Deactivated="Window_Deactivated">

    <Window.Resources>
        <!-- TextBlock default style override — buttons inherit parent Foreground -->
        <Style TargetType="TextBlock"/>

        <!-- Bar button style -->
        <Style TargetType="Button" x:Key="BarButtonStyle">
            <Setter Property="Height"     Value="36"/>
            <Setter Property="MinWidth"   Value="44"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource BtnFgBrush}"/>
            <Setter Property="FontSize"   Value="11"/>
            <Setter Property="Cursor"     Value="Hand"/>
            <Setter Property="Padding"    Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="16"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BtnHoverBgBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BtnPressedBgBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource BtnHoverFgBrush}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{DynamicResource BtnDisabledFgBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Pill background -->
    <Border x:Name="PillBorder"
            CornerRadius="22"
            Background="{DynamicResource PillBgBrush}"
            BorderBrush="{DynamicResource PillBorderBrush}"
            BorderThickness="1"
            Padding="6,4"
            MouseDown="PillBorder_MouseDown">
        <Border.Effect>
            <DropShadowEffect Color="{DynamicResource PillGlowColor}" BlurRadius="18" ShadowDepth="0" Opacity="0.45"/>
        </Border.Effect>
        <StackPanel x:Name="ButtonPanel" Orientation="Horizontal" VerticalAlignment="Center"/>
    </Border>
</Window>
