<Window x:Class="Orbit.RadialMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OrbitMenu" Height="250" Width="250"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Deactivated="Window_Deactivated">
    
    <Window.Resources>
        <Style TargetType="Button" x:Key="RadialButtonStyle">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Background" Value="#AAFFFFFF"/> <!-- Semi-transparent -->
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="25">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFFFFFFF"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Glassmorphism Ring -->
        <Ellipse Fill="#40000000" Stroke="#80FFFFFF" StrokeThickness="2" Width="200" Height="200">
            <Ellipse.Effect>
                <BlurEffect Radius="15"/>
            </Ellipse.Effect>
        </Ellipse>

        <!-- Container for Buttons -->
        <Canvas x:Name="ButtonCanvas" Width="200" Height="200" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Buttons will be dynamically added or pre-defined here.
                 For prototyping, we add some static buttons around the center -->
        </Canvas>
        
        <!-- Center Cursor Indicator (Optional visuals) -->
        <Ellipse Fill="#80FFFFFF" Width="10" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Window>
