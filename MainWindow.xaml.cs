using System.Windows;

namespace Orbital;

/// <summary>
/// MainWindow is currently unused. The application runs primarily from the system tray
/// and displays the RadialMenuWindow on triggers.
/// </summary>
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}